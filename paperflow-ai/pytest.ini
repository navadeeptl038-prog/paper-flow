[pytest]
testpaths = tests
pythonpath = backend
addopts = -ra --strict-markers
markers =
    integration: tests that need live external services or secrets
    requires_config: tests skipped when required env vars are missing
filterwarnings =
    ignore::DeprecationWarning
